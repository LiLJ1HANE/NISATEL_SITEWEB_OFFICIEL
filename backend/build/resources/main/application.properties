# Configuration de base
server.port=8080

# Connexion à PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/nisatel
spring.datasource.username=postgres
spring.datasource.password=Alanas.01

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# CORS (pour le frontend React)
spring.mvc.cors.allowed-origins=http://localhost:3000
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE

# API Security Configuration
app.api.base-path=/api
app.api.key=MIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQCm2
app.api.allowed-origins=http://localhost:3000

# JWT Configuration 
app.jwt.secret=your_jwt_secret_here
app.jwt.expiration-ms=86400000 # 24h
